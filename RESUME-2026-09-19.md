# RESUME — Track F, next session (written 2026-09-18 17:3x, Friday close)

**Check from the artifacts, don't inherit:** `git -C ~/logos-f log --oneline -8` · `python3 freezeck.py` (rc 0) ·
`python3 freezeck.py --selftest` (11/11) · `python3 derive/check.py` (0 mismatch) · `~/logos-dispatch.sh advise`.

## State
- Branch `register-stack`, 17+ commits ahead of origin, **NOT pushed** — the pre-push hook refuses agents; Erik runs
  `git -C ~/logos-f push origin register-stack --no-verify`.
- Freeze (FREEZE-TRACKF.md): static half done. Directive 1 executed (F21 F23 F24 F25 F26 F27 F28a/c F31 F36 fixed);
  cannot-fail clauses 12 → 1 (F28d). Directive 3 done (F37: closure infinite; branch rank 16 FORM / 15 TRUTH).
  Lexicon inventory done (F38).
- ⚠ **NOTHING of today's module edits has RUN.** Modules edited, NOT RUN: lawroot (comments), numderive, opposite, topology,
  texture, entendre, closure, phonometa, unified, recdepth, gapcensus, certify. Static checks clean (laparen, nameck host+VM,
  freezeck, derive/check). The lease was held all day (ELENCHOS, then the General's shadow build).

## FIRST, when the lease is CLEAR (Saturday: POROS P5a runs FIRST — do not jump it)
FREEZE-TRACKF.md §5 run queue, in order: `REGS_VM=0 REGS_KEEP=.freeze-out sh gate_registers.sh` → `python3 checkwants.py --dir
.freeze-out` → `python3 checkreds.py --dir .freeze-out` → V6 clean checkout → V5 `REGS_TWICE=1` → the VM chunks. Any FAIL:
`sh gate_section.sh N` re-runs that section alone. Log every run in §7. Expect the most risk in: recdepth (new binder layout),
entendre (EN_PAY_III rebuilt), certify (field (c) now class-bound; GOOD re-minted), numderive (+12 CONSc).

## Rulings waiting on Erik
F28d (does a declared synonym count as an overload?) · F37 (which reading defines "derived" — proposed interface/FORM ⇒ 16) ·
F38 (Truth ≡ ρ? · Intersubjectivity nesting · κ for Lexicon/Word/Name/Glyph · I = Self vs I→δ · Change's two derivations ·
propagate re-derivations to dependents?) · F30 · F10.

## After the freeze exits
Enter Death (derive the ripple first) · the branch engineering fix (one branch list) · the merge plan
(`~/logos-coord/handoff/2026-09-18-LIEUTENANT-register-stack-merge-plan/README.md`: verified base, freeze lifted, LA_COMPLETION
resolved BY ITEM — about half of its `[ ]` markers are stale against Track F's built modules).
