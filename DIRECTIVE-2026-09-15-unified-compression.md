# DIRECTIVE: THE UNIFIED COMPRESSION — as received 2026-09-15 (evening), with Track F's reconciliation

*Erik's words, via the Observer, are the spec. The reconciliation below is the audit he asked for
("just because I said it does not mean it is correct — correct it against the meta-framework
itself"). Each clause is marked: BUILT · BUILD AS WRITTEN · BUILD CORRECTED (with the correction
and its evidence) · BOUNDED · BLOCKED.*

## The received text (verbatim summary of the clauses that bind the build)
§0 One compression, not twelve: glyphic, semantic, morphological, phonetic, etymological, prosodic,
syntactic, pragmatic, operational compress as ONE act at neologization. §1 Dyadic law: ⊗(A,B)=C is
ONE glyph; |C|≡1; depth grows; recognition of C recovers A,B from the form, not by lookup; gates
gate_dyad_collapse, gate_no_glyphic_entropy. §2 K_unified(A,B,mode) computes every field in one
call; gates gate_unified_compression, gate_no_separate_pass. §3 The mode is the morpheme; Δ_M(Δ_M)≡Δ_M;
gate_mode_genesis (four), gate_morphology_is_glyphs. §4 ν IS the unified compression; Δ_ν extends
the rules; gate_ontoneologization_unified, gate_meta_ontoneologization. §5 onto- prefix closure over
nine registers; gate_onto_prefix_closure. §6 branches: pragmatics, grammatology, grapholinguistics,
discourse, psycholinguistics, sociolinguistics, semiotics, hermeneutics, poetics, ethics; each with a
meta-version and fixed point; gate_branch_registry. §7 Autological: the compiler's own AST dyadic;
K_unified on the compiler's own source yields one unit; etymology readable from the form alone;
gates gate_autological_compiler, gate_autological_collapse, gate_etymology_readable. §8 Syntropy S
rises per collapse; complexity |form|×depth; form/depth decreases; gates gate_syntropy_rises,
gate_no_complexity_growth, gate_centropy_fixed_point. §9 build order. §10 blocked: prosodic,
affective, Hypnos/Oneiros, "full closure of morphogenesis".

## Reconciliation (Track F, before building)

**§0/§2 — BUILD CORRECTED.** The house already has exactly one compression: the SEAL
(`canon.la` COLLAPSE/MONO, `metaglyph.la` SEAL/COLLAPSE — ν). The glyph IS the sealed derivation
tree, and every register is a READING of that one object (SIGIL, PHONYM, CANON, MORPH, LINEAGE…
all walk the same six-constructor node). A `K_unified` that stores `C.form, C.phonym, C.operator,
C.texture…` as FIELDS computed "in one call" would be a step backward: a record of six things is a
coupling, and a second module could fill one field differently — the very thing §2 forbids. The
autological form of the claim is: **K_unified = the seal; there are no fields; each register's
reading of the child is a pure function of the child's seal, and equals one composition law over
the parents' readings.** That is what `neologenesis.la` gates (per register, per birth), and it is
where the "no separate pass" gate lives: a register whose reading needs anything but the seal, or
whose reading of the child is not produced by its law from the parents, goes RED. Pre-registered
finding: the EVIDENTIAL register is NOT born in the movement (how a glyph is KNOWN is assigned by
a gate afterwards), and the pragmatic Γ conjunct inherits that gap. The directive's
`combine_evidential(A.evidential, B.evidential)` would fabricate a witness for an ungated glyph;
it is refused on principle (a W that no gate asserted is the defect class this house exists to
catch). `gate_no_separate_pass` is built as a source gate over the register modules: no function
composes two parents into a register value except through the seal (the composition laws in
neologenesis are PREDICTIONS checked against the seal's reading, never a second producer).

**§1 — BUILD CORRECTED on two numbers.** |C| ≡ 1: TRUE for the seal (one ren, one DAG node added,
sharing allowed) and for the SIGIL (a fixed raster, `sigil.la` SZ). "μ(C) ≥ μ(A)+μ(B)": FALSE as
tree depth — the house's depth is d(C) = 1 + max(d(A), d(B)) (canon TDEPTH, gated in
neologenesis); what IS additive is CONTENT: TSIZE(C) = TSIZE(A)+TSIZE(B)+1. So the law is:
form 1, depth 1+max, content additive. "No glyph's form longer than a primitive's": TRUE for the
sigil raster, and **FALSE for the phonym except under ⊗** — phonym.la's own Operator Phonology
(codex) makes ⊗ a fusion (duration = max) but ⊕ a pause-joined pair (sum+960), ▷ a stress-link
(sum), ⊂ a frame (2B+A), ↻ a reduplication (2A): the phonetic form GROWS with depth for four of
five modes. That is measured in `prosody.la` and will be printed by the entropy gate as a finding
about the codex's phonology, not hidden: only ⊗ compresses the sound; the others couple it.
"Recognition recovers A and B from the form, not by lookup": TRUE for the canonical form
(`lineage.la` RECOVER from the DAG alone; `topoembed.la` mode recoverability for the visual
invariant); NOT YET for the rendered raster — LA_COMPLETION.md's open "Visual round trip — no
bitmap→structure decoder exists" (Tier 2, Track A's sigil domain). Bounded, stated.

**§3 — BUILT.** `modegenesis.la` (Δ_M, four sub-gates, idempotent), the morphological reading in
`registers.la` is the DAG's skeleton (no table), `ontomorph.la` is the census. Add
`gate_morphology_is_glyphs`: every mode in the mode set is a glyph in the DAG with a κ, grounded
in the nine (familytree G1 already covers MODE_*; the gate names it).

**§4 — BUILT under other names.** ν = COLLAPSE/SEAL; Δ_ν = ν* + Δ_M (metaglyph mints a mode from
modes; modegenesis admits it). `gate_meta_ontoneologization` = gate_mode_genesis. Add
`gate_ontoneologization_unified` as a source gate: every seal in the register modules is
MONO(CANON(et))(et) (self-naming) except the declared LIE fixtures.

**§5 — BUILD AS A REGISTRY.** The onto- versions exist as modules already (ontosyntax in the
grammar, ontosemantics = canon, ontopragmatics = pragmatics/ontofelicity, ontophonosemantics =
phonsem/psc, ontotopology = topoembed, ontoetymology = lineage/ancestry, ontomorphology =
ontomorph, ontocompression = the seal, ontoneologization = ν). The registry gate: each entry names
its module and its gate, the module exists, the gate is in build.sh or gate_registers.sh, and the
closure holds (↻-idempotence at the truth level). No new taxonomy is invented.

**§6 — BUILD AS WRITTEN (extend `branchgenesis.la`)** with grammatology, grapholinguistics,
hermeneutics, poetics, ethics (the codex names ontohermeneutics, logopoetics, ontoethics as strata);
each a branch record over the register stack with a declared, collision-checked interface glyph.
Psycholinguistics stays an EMPIRICAL gap (needs speakers) — a record, not a claim.

**§7 — BUILD BOUNDED.** (1) The compiler's own AST: `parser.la` produces Church-encoded
application trees — binary by construction; a gate censuses node arities over the compiler's own
source (`codegen.la`) and reports max arity. (2) "K_unified on the compiler's own source yields one
unit": the house already fuses VM + program into ONE ELF (`bundle.la`, gated in build.sh Stage 5) —
cited as the instance; sealing two source modules as glyphs is not what the compiler does and is
not faked. (3) Modes expressible as modes: MODE_* have decompositions over the nine (metaglyph).
(4)/(5) built (skeleton reading; RECOVER).

**§8 — BUILD CORRECTED.** With |form| ≡ 1, S = content density = TSIZE (unfolded content per
form), which RISES by TSIZE(A)+TSIZE(B)+1 at every collapse; complexity |form|×depth grows by ONE
per nesting; the ratio form/content falls. "depth ≥ 2^n" is not a depth law (depth is n for n
nestings); 2^n is the CONTENT of a full dyadic tree of depth n — the gate states the true
inequality (TSIZE ≥ depth+1, = 2^(d+1)−1 for full dyads). Centropy: Δ_S(Δ_S) ≡ Δ_S by the same
↻-idempotence, and per memory the house has ℭ "already sealed" (Ψ = (S, ℭ)); cited, gated here
only as the fixed point.

**§10 — CORRECTED.** The prosodic and affective registers are BUILT (bounded), not blocked.
The derivation closure is built (`derive_closure.la`): BOUNDED, not closed, and that is the finding.
