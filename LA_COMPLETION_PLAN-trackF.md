# LA COMPLETION — Track F's working plan against the official list (`LA_COMPLETION.md`)

*Erik, 2026-09-15: "dedicate this session and terminal to developing and finishing the programming
language according to everything that I envisioned." The official list is `LA_COMPLETION.md`: 54
open items today (47 `[ ]`, 7 `[~]`), ordered by its own EXECUTION ORDER (Tier 1 root causes →
Tier 2 the three registers → Tier 3 the language in use → Tier 4 self-relation → Tier 5 → the
paper). This file is the work order: what each item needs, whose files it lives in, and what Track F
can build in NEW files versus what needs a grant or a cross-track request. Line numbers are the
ledger's on 2026-09-15 evening (they move; the item titles do not).*

## Standing rules for this work
- One item at a time; each lands with a gate that can go RED and a paper counterpart at the right tag.
- Build in NEW Track-F files wherever the item allows; an item that must edit a Track A/B/C file
  (phonym, sigil, canon, metaglyph, coin, eval…) is a **grant request** in `~/logos-tracks.conf` or
  a board request — never a reach into another track's file.
- Every declared glyph passes `regcollide.py`; every module passes `laparen.py`; every count is
  derived independently before the run (the ontomorph/gramcomplete discipline).
- After each item: run its gate, update `LA_COMPLETION.md` (`[ ]`→`[~]`→`[✓]`, with evidence),
  `ROADMAP.md`, the board.

## Already moved by today's work (to be marked on landing)
| line | item | today |
|---|---|---|
| 1126 | text-level coherence | `textcoherence.la` — built incl. the shuffle-lower criterion → `[✓]` bounded |
| 1175 | Grammar Completeness gate | `gramcomplete.la` → `[✓]` bounded (falsifiable at well-formedness) |
| 1210 | ontomorphology census | `ontomorph.la` → `[✓]` (79 rows, injective 71/71; 8 overloads for ruling) |
| 1086 | one normaliser, not five | `registers.la` NORMTREE ≡ NORMK gate → `[~]` (a sixth exists; reconciliation is the architect's) |
| 1077 | triple bar biconditional | identity-level coherence in `registers.la` → related, NOT closed |
| 1533 | Rule 4 — the neological seal | `gramcomplete.la` witnesses ν as canon's seal (d>0 refused at 0); **write-back into the alphabet still owed** → `[~]` |
| 1287 | executable ν* | `modegenesis.la` admits ν* as a MODE with a usable combinator (MKOP); wiring as an evaluator reduction rule still owed → `[~]` |
| 1295 | operators as glyphs | unchanged `[~]` (Track A's) |

## The order of work, from the top of the list
**Tier 1 — root causes**
1. L786 Three gates that cannot go RED — (c) `seal_test.la` COMPLEXITY constant: Track F can supply
   the real complexity from `unified.la` (|form|×depth, TSIZE) and propose the replacement; (a)/(b)
   are phonym-side (Track A) → request. **Next up.**
2. L770 crossmodal is a report, not a gate — give it a criterion that can fail (rotated-control
   margin) or demote; Track A's module → propose the criterion, request the edit.
3. L801 constant-time `[A]` — Tier 1 ledger row; measurement tooling, not language → defer to ops.
4. L818 Identity Adequacy: three collisions open — `ontomorph.la` now lists all 8 one-κ-two-names
   overloads with names; the RULINGS are Erik's (C9/C10 pattern). → ask for rulings, then re-derive.
**Tier 2 — the three registers** (phonetic/visual: Track A's phonym.la/sigil.la domain)
5. L1021 PSC_STAR pairs raw PHONYM with raw SPEC · L1025 toroidal closure · L1041 ⊗ juxtaposition
   · L1051 catalogue-wide sigil injectivity · L1055 visual round trip · L1060 meta-referent sigil ·
   L853/861/872/879/885/975 phonetic items → each needs a grant on phonym.la/sigil.la/phonseq.la.
   Track F can build the CHECKS in new files (e.g. sigil injectivity over the catalogue by rendering
   through sigil.la's exports; the visual round-trip DECODER as a new module) — the decoder is the
   biggest and unblocks "etymology readable from the raster" (directive §7). **Second up.**
**Tier 3 — the language in use**
6. L1152 lexical depth use-gating (needs a use signal: opgrammar's sentence corpus is one) · L1163
   the four modes of poetic depth (entendre from the DAG: vertical = lineage reading — largely what
   lineage/registers give; three more modes per tex §2250) · L1192 acquisition (needs a syllabus —
   derivable: order concepts by depth from the nine) · L1204 ontofelicity live enforcement (wire to
   logoscap's sealer) — all buildable in new files.
**Tier 4 — self-relation**
7. L1533 Rule 4 write-back (the alphabet must be open: a sealed glyph added to 𝒜 at runtime —
   primitives.la is GENERATED; the honest path is a runtime alphabet register + a gate that a
   sealed glyph is thereafter admitted as a leaf) · L1287 ν* as a reduction rule (evaluator: Track
   C's eval.la → request) · L1290 fractal monoglyph (MONO's etymology slot = the glyphdag form:
   canon.la, Track A → request; Track F can gate it) · L1301/1307/1313/1319/1323 self-meta-
   programming, meta-autopoiesis, wants, AWARE, PROTO_AGENT (the organs — Track F builds gates
   and organs in new files) · L1433/1472/1485/1500/1513/1523 ledger rows (proof-carrying glyphs,
   versioning, ρ(L_t), the self-evolution equation) · L1261/1270/1278 M11b/c, G4 (Track B's, in
   flight on their branch — do not duplicate).
**Tier 5 + paper** — L1558–1583 named, not chased; L1611/1616 the paper's tags track each landing.

## What I will not do
Mark an item `[✓]` whose gate cannot go RED; touch another track's file without a grant; keep a
count by hand that a grep can derive.

## 2026-09-15 late — the metacursive/dyadic question answered (branchclosure.la)
Erik asked whether all branches are dyadic and metacursive, starting from the dyad, with one
unified movement at neologenesis. Answer, gated: (1) dyadic and dyad-grounded — YES, all 19 branch
glyphs ground in the nine, 18 in the ⊂(RELATION,·) interface dyad (branchclosure.la, RED paths).
(2) metacursive — the GENESIS operator Δ_B is (branchgenesis.la, idempotent admission, RED path);
the per-branch ↻↻g≡↻g is VACUOUS (↻-idempotence, all glyphs) and is NOT gated — stated, not hidden.
(3) unified movement — gated at the register level (neologenesis.la, 10/12); branches are joins of
register readings, so it follows but is not separately witnessed. The lexical-depth use-gating row
is NOT WITNESSED (39/78 rotations hold = chance) and stays [A].
