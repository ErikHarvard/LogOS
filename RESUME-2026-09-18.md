# ▶▶ RESUME HERE — Track F, written at close 2026-09-17

**Worktree `~/logos-f`, branch `register-stack`. Six commits ahead of `bc08817`. NOTHING IS PUSHED.**

---

## ‼ FIRST THREE ACTIONS, IN ORDER

### 1. PUSH — it is Erik's to run, and it is blocked by design
```
git -C ~/logos-f push -u origin register-stack --no-verify
```
The repo's `pre-push` hook refuses an agent: *"Publishing is a human decision, and an agent must not make
it. If you are the human and you mean it: git push --no-verify."* I did not bypass it. **Six commits wait:**
```
ec12042  Apply the identity-adequacy rulings to lexicon.la and opgrammar.la
0cab0b5  Defer Liminal and Anamnetic; rule all eight identity-adequacy overloads
2b1de12  The divergent branches: genuinely different compressions do NOT converge on κ
5f1f7ec  Gate 1's positive relation and Gate 3's blindness bound
f812d29  The autological criterion is CO-PRIMITIVE with the Archē
86eac04  Finish Category 1 of the completion list (17 modules)
```

### 2. RE-RUN THE FULL SUITE ON THE CURRENT HEAD
```
cd ~/logos-f && rm -rf .regs_gate_* && REGS_VM=0 sh gate_registers.sh
```
⚠ **The green suite run in `.fullgate.log` is STALE.** It passed (0 FAIL, PASS line) but it started BEFORE
§43–§49 existed and BEFORE the lexicon edits. Per-module and per-mutant checks after those changes are all
green (68 witnesses / 0 missing, 40 RED paths / 0 dead), but the SCRIPT has not run end-to-end on HEAD.

### 3. RUN THE VM LEGS — **NOT ONE of the 19 new modules has ever run on the SECD VM**
Host and VM must be byte-identical. `REGS_VM=1` covers the light list; the new modules are in it.
This is the single largest unverified surface.
> ✗ **CORRECTED 2026-09-18:** the line above was FALSE when written — the light list held only
> `recdepth`/`selfevo`, so `REGS_VM=1` would have gone green without touching 17 of the 19. They are
> now added (33 light modules; closures 47–213 glyphs; all pass `nameck.py --vm`). **The VM leg's wall
> for 33 modules is UNMEASURED** — measure it on the first run, do not budget it by reasoning.
> Also 09-18: `nameck.py` exited 0 on findings (a `|| echo FAIL` sweep was vacuous) — now exits 1,
> and `--host`/`--vm` exist. `syllabus.la` no longer exports the undefined `SYLLABUS` (host re-run owed).
> **CHUNKED VM LEG (09-18):** `REGS_VM_CHUNK=k/n sh gate_registers.sh` runs ONLY slice k of n of the VM
> list — §1–§10 skipped — so it fits a short deep slot. Preview a slice free: add `REGS_VM_PLAN=1`.
> Its PASS line says `registers/vm chunk k/n` and is NOT the gate's PASS. Every module prints a
> `TIME` line (codegen/vm/host seconds) — size later chunks from those. Plumbing verified with stub
> binaries only (green, drift, codegen-fail, all n=1..33/37 partitions, refusals); never run for real.

---

## THE TOOLS THAT MAKE THE ABOVE FAST (all committed)
| tool | what it does |
|---|---|
| `nameck.py` | static unbound-name check. **Run it before every host run** — a 1-second check that catches what a 9-minute run would. |
| `laparen.py` | per-definition paren balance. |
| `regcollide.py` | collision-check a proposed κ against the whole corpus. |
| scratch `checkwants.py` / `checkreds.py` | compare every `want`/`red` token in the gate against captured output. **In `/tmp/.../scratchpad/` — COPY THEM INTO THE REPO, they are not committed and the scratchpad is session-local.** |

---

## WHAT IS DONE (49 gate sections; 68 witnesses / 0 missing; 40 RED paths / 0 dead)

**CATEGORY 1 OF `LA_COMPLETION.md` IS COMPLETE AND VERIFIED** — all ten items:
ρ(L_t) §31 · Self-Evolution Equation §32 · Seal 2 proof-carrying glyphs §33 · Seal 3 versioning §34 ·
Meta-Ontosemantic Closure §35 · κ* §36 · the Substitution Test §37 · ontosemiosyntax §38 · Δ_ν §39 ·
the metacursive phonosemantic level §40.

**THE FRAMEWORK GATES** (built in answer to Erik's questions): the identity relation as a glyph §41 ·
the Shannon/compression bound §42 · logic-algorithm-syntax §43 · the Three Laws vs the Archē §44 ·
⊗-idempotence uniqueness §45 · cross-branch collapse §46 · number + the 2^(d+2)−1 formula §47 ·
divergent branches §48 · the identity-adequacy rulings §49.

---

## ▶ WHAT REMAINS — THE ACTUAL WORK QUEUE

### A. OWED BY ERIK (each blocks real work)
1. **THE SEAL-1 TYPE RULING.** Three candidate type systems, one implemented. The tex §5012 Ontic Type
   System (τ ∈ {Object, Process, Relation, Value, Constraint}) has **zero occurrences in code**;
   `TYPE_SYSTEM_SPEC.md` proposes a different mode-indexed system and adds no code; what is BUILT is
   DEPLOY's arrow-arity check. **Blocks:** field (a) of every proof-carrying certificate (§33). The slot
   is already shaped; naming one system unblocks it.
2. **Liminal / Anamnetic / Logorhetoric / Meta-Rosettology / Adamic** — now formally DEFERRED (not
   "blocked"), with the end condition written into `LA_COMPLETION.md`: a branch needs (i) a definition of
   what it studies and (ii) an interface mapping, the operand `x` of its dyad `⊂(RELATION,x)`. Until then
   **the branch count is 19** and every cross-branch claim is bounded to it.
3. **Ratify or reverse the eight adequacy rulings** (§49, now APPLIED to the vocabulary). Each is one line
   to reverse and its reason is stated beside it.

### B. IMMEDIATE FOLLOW-THROUGH ON TODAY'S WORK
4. **`syllabus.la` exports `SYLLABUS`, which nothing defines.** Found by `nameck.py`; committed in
   `cc906b3`, gated §23. A one-line fix + re-run. Same defect class I fixed in `selfevo.la`.
5. **Re-render the seven changed lexicon entries.** `sigil.la` and `phonym.la` walk κ-decompositions, so
   they pick up the new forms automatically — but nobody has LOOKED at the new sigils/phonyms.
   `Bond a[ra]a · Sky ta[ha]ta · Move v'uta · Good alu · Large dota · Here t'ara · There ra[ha]ra`.
6. **`lexdepth.la` moved from 39/78 to 44/78** after the lexicon edits. Verdict unchanged (NOT WITNESSED,
   chance≈39) and it is **not in the gate at all** — consider gating it, or note why not.

### C. CATEGORY 2 — needs a grant in `~/logos-tracks.conf` or a cross-track merge
7. Phonetic/visual (Track A `phonym.la`/`sigil.la`): acoustic signature · elision layer · phonseq ▷ marker
   · PSC_STAR raw pairing · toroidal closure · ⊗-as-fusion · catalogue-wide sigil injectivity ·
   **the visual round-trip DECODER** (unblocks "etymology readable from the raster") · **the meta-referent's
   sigil** — ★ its gate is already SPECIFIED at `LA_COMPLETION.md:1067` and the uniqueness it would witness
   is already enforced by `canon.la`'s `REWRITE_SYN`. Best next Category-2 item.
8. M11b ancestry walk / M11c auto-registration / Gate G4 (Track B, in flight).
9. Rule 4 write-back — ELENCHOS's M58, landed `6a23dfe` on their branch, **needs a merge**.
10. self-meta-programming (execve the adopted organ); the meta-autopoiesis gate.
11. The four audit operators |G|, |G_meta|, ς, μ — still exist nowhere.

### D. CATEGORY 3 (external, not buildable) / CATEGORY 4 (ceilings — do NOT chase)
Acquisition's empirical half (needs a speaker) · the trans-species FUNCTOR (needs a second channel) ·
entropy on metal + the signature scheme (Track E) · empirical calibration.
Ceilings: derivation closure 4/9 · constant-time [A] · lexical-depth use-gating (chance) · Grammar
Completeness (bounded) · qualia.

### E. THE OPTIMIZATION TRACK — now with measured evidence behind it
12. **A memoizing/hash-consing evaluator in `tiny_host.c`.** Measured twice today: **a named glyph is
    re-evaluated at EVERY reference**, so a `read_file` in a glyph body is re-read per reference.
    `closure.la` went from *never finishing* to **1m36s** once the value was bound once and threaded.
    This is the hot path, not a nicety.
13. Faster/cached SECD codegen (dominates every VM leg — and the VM legs are item 3).
14. A dynamic glyph table (removes the 1024 ceiling and the re-export gymnastics).

### F. THE PAPER
15. Fix "trimodal" wherever a fourth modality exists; every Tier-1/2 item needs its paper counterpart at
    the right tag. **★ And the paper now owes several CORRECTIONS this session earned — see below.**

---

## ★★ FINDINGS THE PAPER/CORPUS MUST ABSORB (each is gated, each corrects a stated claim)

1. **The Three Laws are NOT derived from the Archē** (§44). Tested by REMOVAL with a control proving the
   removal is real. `OUTLINE.md` §19.4's *"derived from ∃(∃)≡∃, not assumed"* is not what the code does.
2. **The autological criterion is CO-PRIMITIVE with the Archē too** (§44). So the Archē, the three laws
   and the criterion are **FOUR INDEPENDENT GROUNDS — the ground is a SMALL SET, not a single point.**
   Consistent with `derive_closure.la`: 4 of 9 primitives derive, 5 are named axioms.
3. **Divergent compressions do NOT converge on κ** (§48). Six constructed compressions give six distinct
   outputs; five of six never reach a fixed point; the only one that rests is DELETION, which rests by
   discarding. ⇒ **CONVERGENCE AND RETENTION ARE IN TENSION. Rest is bought with loss.**
4. **The cross-branch collapse is BY CONSTRUCTION, not convergence** (§46) — one sealer, 18 branches, and
   there are **19 branches, not 28**.
5. **Logic / algorithm / syntax are pairwise SEPARATE** (§43). The strong reading is false. The true
   relation is the **NEOLOGIZING DYAD**: 𝔑(A,B) with A ≢ B and both recoverable.
6. **BEING IS NOT ONE** (§47). It is the identity combinator; one = BECOMING(VOID). Unity is DERIVED.
7. **THE MEASURE HORIZON** (§47): four κ-distinct forms share one numeral, so arithmetic's reach stops
   where the mode begins. Measure says HOW BIG a form is, never WHICH form it is.
8. **Shannon: no noise in the computational register** (§42); the only real bound is the RENDERER's
   capacity, and `sigil.la` walks the UNFOLDED decomposition so it inherits the exponential one. A
   renderer encoding the DAG would survive ~40× deeper. **That is a concrete design fix available now.**
9. **The three collisions Erik named do NOT exist** (§49) — Change/Can differ in operand order,
   Give/Because and Know/You in mode, and ontosynthesis is non-commutative per tex:2837.

---

## ⚠ TRAPS THAT COST TIME TODAY — READ BEFORE WORKING
- **`pkill -f <pattern>` KILLS THE ISSUING SHELL** (the pattern matches its own command line). Surfaces as
  a bare exit 144 with the compound half-run. Kill by PID with a non-self-matching pattern.
- **A later `glyph X = …` does NOT shadow an imported X** — `tiny_host` returns the FIRST definition. A
  mutant that redefines an imported glyph **changes nothing and its RED path silently never fires.**
  Route every asserted import through a LOCAL ALIAS.
- **Duplicate gate output tags**: two `host` lines sharing a tag write the same `$T/<tag>` file, so a
  section can be checked against the WRONG module's output. Sweep with
  `grep -oE '^host [^ ]+ [a-z_0-9]+' gate_registers.sh | awk '{print $3}' | sort | uniq -d`.
- **Astral-plane characters need `\U0001D4DC`, not `\u1d4d`** — a 4-digit escape silently produced a
  different character and a module about 𝓜 printed ᵍ.
- **Derive expected values BEFORE writing the gate.** Every module matched on its FIRST run because of
  this. Where a derivation and a module disagreed, it was twice MY derivation that was wrong.
